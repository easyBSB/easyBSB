{
  "executors": {
    "i18n-sync": {
      "implementation": "./i18n/bin/i18n-sync.executor",
      "schema": "./i18n/schema.json",
      "description": "synchronize i18n files"
    },
    "playwright": {
      "implementation": "./playwright/bin/playwright.executor",
      "schema": "./playwright/schema.json",
      "description": "Runs `echo` (to test executors out)."
    },
    "serve-e2e": {
      "implementation": "./api-server-e2e/bin/api-server-e2e.executor",
      "schema": "./api-server-e2e/schema.json",
      "description": "Runs `echo` (to test executors out)."
    }
  }
}
